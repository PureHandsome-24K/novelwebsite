#配置数据源
spring:
  datasource:
    hikari:
      connection-timeout: 30000
    username: root
    password: 122512
    url: jdbc:mysql://localhost:3306/novel_db?serverTimezone=Asia/Shanghai&useUnicode=true&characterEncoding=utf-8&useSSL=false&allowPublicKeyRetrieval=true
    driver-class-name: com.mysql.cj.jdbc.Driver
  messages:
    basename: i18n/login
    encoding: UTF-8
    fallback-to-system-locale: true
  thymeleaf:
    cache: false

mybatis-plus:
  mapper-locations: classpath*:/mapper/**/*.xml
  type-aliases-package: com.liu.entity